# Python Azure Functions requirements - Minimal set to avoid grpcio issues
# Updated: July 29, 2025 - Minimal dependencies to avoid compilation errors
azure-functions
azure-storage-blob
azure-identity
requests

# Test dependencies
pytest
azure-functions-worker
