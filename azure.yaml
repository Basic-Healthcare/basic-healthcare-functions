# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: basic-healthcare-functions
metadata:
  template: basic-healthcare-functions@0.0.1-beta
  
infra:
  provider: terraform
  path: infra

services:
  function-app:
    project: .
    language: py
    host: function
    
pipeline:
  provider: github
